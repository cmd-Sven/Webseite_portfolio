# Blog-Cover

Lege pro Beitrag ein Bild ab, z. B. `cro-hypothesen.webp`, und trage den Pfad in `src/data/blogPosts.ts` unter `coverImage` ein.

Ohne `coverImage` wird `/blog/placeholder.svg` verwendet.
